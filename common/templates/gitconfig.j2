# Managed by Ansible

[core]
  editor = vim
  whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
  excludesfile = ~/.gitignore

[alias]
  co = checkout
  ci = commit
  rb = rebase
  st = status
  cl = clone
  setpro = config user.name "Vincent Aaron Pro" && config user.email "vincent.aaron@company.com"
  setper = config user.name "Vincent Aaron" && config user.email "piliman974@gmail.com"


[color]
  status = auto
  branch = auto
  ui = auto

[user]
  name = {{ git_user_name }}
  email = {{ git_user_email }}
  username = {{ git_user_username }}

[mergetool "vimdiff3"]
   cmd = vim -f -d -c "wincmd J" "$MERGED" "$LOCAL" "$BASE" "$REMOTE"

[github]
  user = {{ github_user }}

[http]
  postBuffer = 157286400

[includeIf "gitdir:~/work/"]
  path = ~/.gitconfig-work

[includeIf "gitdir:~/projects/"]
  path = ~/.gitconfig-personal